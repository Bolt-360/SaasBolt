'use strict';

var define = require('define-properties');
var getPolyfill = require('es-iterator-helpers/Iterator.prototype.filter/polyfill');

var $IteratorPrototype = require('es-iterator-helpers/Iterator.prototype/implementation');

module.exports = function shimIteratorPrototypeFilter() {
	var polyfill = getPolyfill();

	define(
		$IteratorPrototype,
		{ filter: polyfill },
		{ filter: function () { return $IteratorPrototype.filter !== polyfill; } }
	);

	return polyfill;
};
